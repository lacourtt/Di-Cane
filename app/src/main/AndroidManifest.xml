<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.DiCane"
        tools:targetApi="31">
        <activity
            android:name=".dog.DogDetailsActivity"
            android:exported="false"
            android:label="@string/title_activity_dog_details"
            android:theme="@style/Theme.DiCane" />
        <activity
            android:name=".buy.BuyActivity"
            android:exported="false"
            android:label="@string/title_activity_buy"
            android:theme="@style/Theme.DiCane" />
        <activity
            android:name=".breeder.BreederHomeActivity"
            android:exported="false"
            android:label="@string/title_activity_breeder_home"
            android:theme="@style/Theme.DiCane" />
        <activity
            android:name=".litter.LitterDetailsActivity"
            android:exported="false"
            android:label="@string/title_activity_litter_details"
            android:theme="@style/Theme.DiCane" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DiCane">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>